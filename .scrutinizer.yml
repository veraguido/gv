checks:
    php: true

build:
  nodes:
    my-tests:
      environment:
        php:
          version: 7.4
    analysis:
      environment:
        php:
          version: 7.4
      dependencies:
        after:
          - composer require --dev squizlabs/php_codesniffer:2.9
      tests:
        override:
          -
            command: phpcs-run
            use_website_config: false