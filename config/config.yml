config:
  devmode: false
  mysql:
    host: mysql
    driver: pdo_mysql
    db_name: gv
    username: root
    password: root
  redis:
    host: redis
    port: 6379
    cluster: false
    pool_size: 20
    username:
    password:
  email:
    host: lala.example.com
    smtp_auth: true
    username:
    password:
    smtp_secure:
    port:
  allowed_upload_file_types:
    - text/csv
    - image/png
    - image/jpeg
    - image/gif
